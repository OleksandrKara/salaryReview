services:
  postgres:
    image: postgres:16
    container_name: salonreview-postgres
    environment:
      POSTGRES_DB: salonreview
      POSTGRES_USER: salon
      POSTGRES_PASSWORD: salon
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U salon -d salonreview"]
      interval: 5s
      timeout: 5s
      retries: 10

  backend:
    build:
      context: ./backend
    container_name: salonreview-backend
    environment:
      # Override the dev defaults in application.yml — talk to the postgres service via Docker DNS.
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/salonreview
      SPRING_DATASOURCE_USERNAME: salon
      SPRING_DATASOURCE_PASSWORD: salon
      # Square credentials — read from your shell or the .env file next to this compose file.
      SQUARE_ENVIRONMENT: ${SQUARE_ENVIRONMENT:-sandbox}
      SQUARE_ACCESS_TOKEN: ${SQUARE_ACCESS_TOKEN:-}
      SQUARE_LOCATION_ID: ${SQUARE_LOCATION_ID:-}
      # Single shared owner login (Phase 1).
      APP_OWNER_USERNAME: ${APP_OWNER_USERNAME:-owner}
      APP_OWNER_PASSWORD: ${APP_OWNER_PASSWORD:-changeme}
    ports:
      - "8080:8080"
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/actuator/health"]
      interval: 5s
      timeout: 3s
      retries: 20
      start_period: 30s

  frontend:
    build:
      context: ./frontend
    container_name: salonreview-frontend
    environment:
      # Server-side fetches (Node in the container) → talk to backend over Docker DNS
      BACKEND_URL: http://backend:8080
      # Browser-side fetches → user's machine hits host port 8080
      NEXT_PUBLIC_API_BASE: http://localhost:8080
    ports:
      - "3000:3000"
    depends_on:
      backend:
        condition: service_healthy

  adminer:
    image: adminer:latest
    container_name: salonreview-adminer
    restart: unless-stopped
    ports:
      - "8081:8080"
    environment:
      ADMINER_DEFAULT_SERVER: postgres
      ADMINER_DESIGN: pepa-linha
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  postgres-data:
